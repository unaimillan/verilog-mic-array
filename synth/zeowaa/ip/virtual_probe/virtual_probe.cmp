	component virtual_probe is
		port (
			probe  : in  std_logic_vector(63 downto 0) := (others => 'X'); -- probe
			source : out std_logic_vector(63 downto 0)                     -- source
		);
	end component virtual_probe;

